# Ops Assets

This folder contains operational assets for running Shadow API with Prometheus + Grafana.

## Contents

- `alerts/prometheus-rules.yaml` — sample Prometheus alert rules (includes runbook URLs)
- `grafana/shadow-api-dashboard.json` — sample Grafana dashboard

## Metrics Endpoint

Shadow API exposes Prometheus metrics at `GET /v1/metrics`.

